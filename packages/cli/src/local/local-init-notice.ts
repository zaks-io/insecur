/** One-line Local Mode notice printed after unauthenticated or `--host local` init. */
export const LOCAL_INIT_NOTICE =
  "Local Mode: secrets stay on this machine only (no account). Run insecur login for sync and hosted controls.";
