{
  "name": "@insecur/instance-bootstrap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "test": "vitest run --config vitest.config.ts",
    "test:integration": "vitest run --config vitest.rls.config.ts",
    "test:rls": "vitest run --config vitest.rls.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@insecur/access": "workspace:*",
    "@insecur/audit": "workspace:*",
    "@insecur/auth": "workspace:*",
    "@insecur/domain": "workspace:*",
    "@insecur/tenant-store": "workspace:*",
    "@insecur/token-signing": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
